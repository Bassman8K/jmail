<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      A bare splash theme only. Everything visual comes from Compose, but the window
      background must be set here or the app flashes white (or black, in dark mode) between
      the launcher animation and the first frame.
    -->
    <style name="Theme.JMail" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
